apiVersion: v1
kind: Pod
metadata:
  name: database-app
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
  volumes:
    - name: storage-volume
      persistentVolumeClaim:
        claimName: pvc
